# Phase 6 — Fix Request

Generated by `/check phase-6` on 2026-07-17.
Gate verdict: `changes_requested`.
Reason: one fixable coverage gap plus documentation corrections required so the evidence accurately reflects the code change set. No unrelated drift.

## Issue 1 — CHK-001 (medium, fixable)

- id: `CHK-001`
- short title: `cli.ingest <source_id>` linked-inbox-item branch lacks automated regression
- problem target:
  - `src/llm_wiki/cli.py:863-922` (the `cli.ingest <source_id>` branch when `_linked_inbox_item_for_source` returns a non-`None` inbox item).
  - `tests/test_cli_inbox.py` (no test exists for this branch).
- reason:
  - `check-code-stability` reported that only the no-linked-item branch (`test_ingest_source_id_without_linked_inbox_item_keeps_legacy_raw_source`) is covered. The realistic re-ingest scenario where an operator re-runs `wiki ingest <source_id>` against an already-registered inbox item (so `_linked_inbox_item_for_source` is non-`None`) is not exercised.
  - The archive-finalization patch routes this branch through `_finalize_successful_inbox_ingest`, so any regression here would silently break the success route.
- improvement spec:
  - Add a test in `tests/test_cli_inbox.py` (e.g. `test_ingest_source_id_with_linked_inbox_item_finalizes_archive_and_keeps_state`) that:
    1. Initializes a fresh `WikiPaths` under `tmp_path` and registers a markdown via `inbox.register_markdown_file`.
    2. Calls `inbox.materialize_source_for_inbox_item(paths, item.id)` so a `sources` row and `source_id` exist.
    3. Monkeypatches `OllamaClient` (or `cli.OllamaClient`) and `ingest_llm.ingest_source` to return an `IngestResult(ok=True, pages_created=N, pages_updated=0, source_id=<sid>, source_title=<title>, source_slug=<slug>)`.
    4. Invokes the `wiki ingest <source_id>` CLI command via `typer.testing.CliRunner` (or directly through the public function `_process_inbox_item` after setting `inbox_item=_linked_inbox_item_for_source(paths, source_id)`).
    5. Asserts:
       - `ingest_llm.ingest_source` was called once with the materialized `source_id`.
       - The inbox item is `INGESTED`, relpath points to `raw/<name>`.
       - `sources.relpath` is `raw/<name>`.
       - `inbox_events` tail for the item ends with `["moved_to_archive", "cli_ingest_completed"]`.
       - The original file is no longer under `paths.inbox_markdown/<name>` and a copy exists at `paths.raw_archive/<name>`.
- suggested build agent: `build-test-validation`
- validation required:
  - `pytest tests/test_cli_inbox.py -v` — new test passes.
  - `pytest tests/test_cli_inbox.py tests/test_inbox_to_job_mapping.py tests/test_inbox_domain.py tests/test_inbox_registration.py tests/test_phase4_review_failed_workbench.py tests/test_web_navigation.py` — full focused set still 44+ passed.
  - `py_compile tests/test_cli_inbox.py` exit 0.
- acceptance criteria:
  - New test name listed in `tests/test_cli_inbox.py` and reproducible from the focused pytest command above.
  - The new test asserts archive move + INGESTED state + `sources.relpath` alignment + event-type ordering.
  - No change to production code is required; this is a test-only patch.

## Issue 2 — CHK-002 (low, fixable)

- id: `CHK-002`
- short title: Phase 6 evidence incorrectly states "no code was changed"
- problem target:
  - `.code-planner/03-build/evidence/phase-6-build-evidence.md` (line ~30-50, the "No code was changed in Phase 6" / "No commits" sections).
  - `.code-planner/03-build/evidence/phase-5B-build-evidence.md` ("Files changed" / "No modifications to" lines that explicitly listed inbox.py).
- reason:
  - `check-change-scope` flagged that the archive-finalization patch added new code (`inbox.finalize_successful_ingest`, `_is_within_directory`, plus call sites in `cli.py` and `jobs.py`) during Phase 6 to unblock E2E rows B-7 / C-3. The Phase 6 evidence still claims "Phase 6 has not modified any tracked source or test file" which is factually incorrect.
  - Phase 5B evidence stated "No modifications to `inbox.py`", which is now stale.
- improvement spec:
  - In `phase-6-build-evidence.md`:
    - Replace the "No code was changed in Phase 6" claim with an explicit "Archive-finalization deliverable added in-flight during Phase 6" section that:
      - Names the new function `inbox.finalize_successful_ingest` and the new helper `_is_within_directory`.
      - Lists the call sites in `src/llm_wiki/cli.py:179-194`, `src/llm_wiki/cli.py:884-909`, `src/llm_wiki/jobs.py:536-548`.
      - Records the rationale: B-7 / C-3 of the E2E checklist were failing because the success route did not move the original Inbox file into `raw/` or record `moved_to_archive`.
      - Updates the `git diff --stat HEAD` snapshot to reflect the new 8-file / +660/-85 diff.
    - Update the "Validation-plan coverage" table to mark B-7 / C-3 as PASS with the archive finalization row referenced.
    - Update the "Ready for `/check phase-6`" verdict to reflect that the close is partial pending CHK-001.
  - In `phase-5B-build-evidence.md`:
    - Add a footnote: "Note: `src/llm_wiki/inbox.py` was later modified as part of Phase 6 (archive-finalization prerequisite). See `phase-6-build-evidence.md` §'Archive-finalization deliverable'."
- suggested build agent: `build-test-validation`
- validation required:
  - Manual read-through of both evidence files against the current diff (`git diff --stat HEAD`).
  - Confirm both documents no longer contain the now-incorrect "No code was changed" / "No modifications to inbox.py" lines.
- acceptance criteria:
  - `phase-6-build-evidence.md` has a clearly labeled "Archive-finalization deliverable" section.
  - `phase-5B-build-evidence.md` has a clearly labeled "Phase 6 follow-up" footnote on inbox.py.
  - The "Ready for `/check phase-6`" verdict and the "Risks" list reflect CHK-001 / CHK-002 / CHK-003.

## Issue 3 — CHK-003 (note, optional)

- id: `CHK-003`
- short title: Maintainability cleanups (STAB-003 / STAB-004)
- problem target:
  - `src/llm_wiki/cli.py:147-153` (`_linked_inbox_item_for_source`) and `src/llm_wiki/jobs.py:252-258` (`_linked_inbox_item_id_for_source`) — near-duplicate `SELECT id FROM inbox_items WHERE source_id = ? ORDER BY id DESC LIMIT 1` queries.
  - `src/llm_wiki/cli.py:815-817` — orphaned lowercase sentence in the `cli.ingest` docstring.
- reason:
  - `check-code-stability` reported these as low / note severity. They do not affect behavior, but consolidating the duplicated query avoids future SQL drift between the two call sites, and the docstring fix improves readability.
- improvement spec (optional):
  - Add `inbox.linked_inbox_item_id_for_source(paths, source_id) -> int | None` (returning the raw id) or `inbox.linked_inbox_item_for_source(paths, source_id) -> InboxItem | None` (returning the full item) in `src/llm_wiki/inbox.py`. Have both `cli._linked_inbox_item_for_source` and `jobs._linked_inbox_item_id_for_source` delegate to it.
  - Stitch or remove the orphaned lowercase sentence in the `cli.ingest` docstring.
- suggested build agent: `build-core-dev`
- validation required:
  - `pytest tests/test_cli_inbox.py tests/test_inbox_to_job_mapping.py tests/test_web_navigation.py` — full focused set still passes.
  - `py_compile` clean.
- acceptance criteria:
  - Optional. If folded into CHK-001/CHK-002, both call sites route through the new helper and no longer contain the raw SQL.
  - The cli.py:815-817 docstring no longer has the orphaned sentence.
