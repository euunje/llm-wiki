# Knowledge Pipeline

A short sample used to exercise the Phase 1 pipeline. The Markdown body is intentionally
small so chunking produces exactly one chunk under default settings.

The pipeline records every step in SQLite and produces an artifact report so the CLI
can be smoke-tested without launching a long-running service.
